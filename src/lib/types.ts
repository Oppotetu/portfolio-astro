export interface Project {
  slug: string;
  title: string;
  authors: string[];
  oppsummering: string;
  squareFootage: number;
  publisert: string;
  assignmentType: string;
  // gallery: { ref: string }[];
}
